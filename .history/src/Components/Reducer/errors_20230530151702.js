const initialState = {
    key: "*",
    name: "*",
    phoneNumber: "*",
    email: "*"
}

export const errors = (state = initialState, { type, payload }) => {
    // switch(action.type) {
    //     case "HANDLE_ERROR": {
    //         let errors = {...state};
    //         errrors[]
    //     }
    // }
    return state
}
